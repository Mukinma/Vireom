# Inter local fonts (required)

Download and store these exact files in this folder:

- `Inter-Regular.woff2` (weight 400)
- `Inter-Medium.woff2` (weight 500)
- `Inter-SemiBold.woff2` (weight 600)

Recommended source package version: Inter v4.1 static web fonts.
